---
- name: Install Packages
  ansible.builtin.apt:
    name:
      - ntp
      - python3-pip
      - virutalvenv
      - python3-setuptools
      - tcpdump
      - wget
      - openssl
      - curl
    state: present
    update_cache: true
